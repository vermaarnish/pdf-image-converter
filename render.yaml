# Render Blueprint configuration for automated deployment
services:
  - type: web
    name: pdf-image-converter
    env: docker
    plan: free
    # Port will be dynamically injected by Render
    envVars:
      - key: PORT
        value: 8080
